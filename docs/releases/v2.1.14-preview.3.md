# AISC Workbench v2.1.14-preview.3

> **Preview**（develop 分支预发布，prerelease）。
> **自动更新验证版**：与 preview.2 同源码，专为验证 v2.1.14 应用内自更新链而发（preview.2 装机 → 检查更新 → 本版，一次验证下载完整/staging 命名/清场/静默安装/自动重启全链）。

## 相对 v2.1.14-preview.2

无代码变更（同源码打 tag 验证更新链）。

## 验证要点（preview.2 装机视角）

1. 检查更新应检出本版（同 base 高 preview 号）；
2. 下载过程不中断（b12 超时修复）；
3. `%TEMP%\aisc-workbench-update\` 中暂存文件名为 `workbench-setup-2.1.14-preview.3.exe`（目标版本命名），且旧残留（2.1.12.exe / 2.1.13-preview.1.part）被自动清理；
4. 确认「退出并更新」后：应用退出 → 静默安装 → Docker 升级链（镜像重建，最长 30 分钟）→ **自动重启**（无需手动打开）；
5. 重启后 About 显示 `2.1.14-preview.3`，进程无管理员标记。

## 安装 / 更新

- v2.1.14-preview.2 用户：应用内「检查更新」即可（全自动）。
- 其它版本：浏览器下载手动安装。

## CLI 更新通道

- 随安装器自动更新（CLI 0.1.2）；pip 用户 `pip install -U aisc-cli`。
