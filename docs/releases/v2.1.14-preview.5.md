# AISC Workbench v2.1.14-preview.5

> **Preview**（develop 分支预发布，prerelease）。
> **进度条验证版第二跳**：从 preview.4 应用内更新至本版，验证 b13 进度条 + 全自动更新链在日常迭代中的表现。

## 相对 v2.1.14-preview.4

无代码变更（同源码打 tag 验证更新链）。

## 验证要点（preview.4 装机视角）

- 应用内「检查更新」→ 检出本版 → 下载（**进度条**应为 MB 格式 + 可视填充条）→ 确认 → 静默安装 → Docker 升级链 → 自动重启 → About = 2.1.14-preview.5。

## 安装 / 更新

- v2.1.14-preview.4 用户：应用内「检查更新」即可（全自动）。

## CLI 更新通道

- 随安装器自动更新（CLI 0.1.2）；pip 用户 `pip install -U aisc-cli`。
