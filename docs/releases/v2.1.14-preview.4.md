# AISC Workbench v2.1.14-preview.4

> **Preview**（develop 分支预发布，prerelease）。
> **进度条验证版**：带 b13 下载进度 UX（MB/GB 格式 + 可视进度条）。

## 相对 v2.1.14-preview.3

- **下载进度人性化**（b13）：`292726047 B` 式裸字节数改为 `279.1 MB / 279.1 MB (100%)` + CSS 可视进度条（accent 填充，实时推进）
- **确认弹窗**补「桌面无进度提示属正常」说明（消除升级链等待期间「随机唤醒」感）

## 验证要点（preview.3 装机视角）

1. 检查更新 → 下载 → 进度条应显示**可视化进度**（非裸字节数），格式为 MB + 百分比；
2. 其余同 preview.3（下载完整 / staging 命名 / 静默安装 / 自动重启全链——上一跳已全部验证通过）。

## 安装 / 更新

- v2.1.14-preview.3 用户：应用内「检查更新」即可（全自动）。

## CLI 更新通道

- 随安装器自动更新（CLI 0.1.2）；pip 用户 `pip install -U aisc-cli`。
